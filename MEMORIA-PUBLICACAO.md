
# Publicacao frontend — 23/09/2026
Usuario autorizou publicar todos os sites. Publicar reconstrucao do painel e cardapio existentes (rotas legadas) e fontes V2 protegidas por opt-in. Nao habilitar VITE_V2_HOSPEDADO, nao implantar Functions/regras/indices nem criar loja remota nesta rodada. /gestao-v2 segue informando ambiente nao habilitado ate preparar backend. Build de producao executado; testes completos ficam para amanha. Segredos, fixtures e arquivos locais functions/.env nao entram no commit.

## 23/09/2026 — SVG restantes
`src/lib/icons.js`: SVGs de fechar/alimentos/bebidas/hambúrguer. `src/pages/painel-grupos.js`: fechar editor/opção usa SVG. `src/lib/grupos.js`: fallback de categorias usa SVG em lugar de emoji. As demais interfaces do cardápio já usavam o conjunto `ico`; site oficial também já usa SVG.
Validação: `npm run build` aprovado (aviso existente de chunk >500KB). Nenhuma mudança em API, dados, autenticação, regras ou ativação do backend V2.

## 23/09/2026 — Organização do catálogo, pedidos e entregas por bairro
- `src/pages/painel.js` e `painel-catalogo.css`: submenu de categorias com contagem, primeira categoria selecionada, logo da loja obtido da licença/backup do PDV, FlowPDV centralizado. Cartões com foto maior à esquerda, controles da foto abaixo, opções à direita e descrição na largura inteira. Endereço separado, máscara de WhatsApp e cadastro de taxas/repasse por bairro.
- `src/styles.css`: bloqueio de rolagem do modal sem criar um ancestral de rolagem no body; menu lateral mantém posição ao abrir itens distantes. Estilos dos campos de entrega. `cardapio-design.css`: checkout utilizável em paisagem, com conteúdo rolável e rodapé acessível.
- `src/lib/pedido-ui.js` e `painel.js`: cozinha e histórico sem fotos, quantidade/adicionais/observação separados. Endereço e repasse exibidos apenas no painel privado da loja.
- `shared/entrega.js`, `src/lib/overlay.js`, `src/pages/cardapio.js`, `src/pages/pedido.js`, `api/criar-pedido.js`: entregas desativadas por padrão, configuração por bairro, escolha de retirada/entrega e endereço no checkout. Taxa e total calculados novamente no servidor, bairro não atendido recusado, repasse e identificação/endereço excluídos da resposta e do documento público. Repasse é referência interna, não pagamento automático nem gestão de motoristas. Mesa/retirada continuam sem taxa de entrega.
- Validação: sete testes Node em `tests/entrega.test.js` aprovados (contato, configuração, privacidade, cálculo no servidor, área atendida, retirada e reenvio idempotente); build de produção aprovado, com aviso anterior de chunk >500KB. Fixture offline com código real e dados sintéticos em 390x844, 844x390 e 1366x768: sem erros JavaScript/overflow horizontal, sidebar y=0 ao abrir modal após rolagem e rolagem restaurada ao fechar; configuração/checkout exercitados. Evidências locais em `flowpdv-sistema/output/reconstrucao-web-20260923/cardapio-painel/`.
- Limites: testes não criaram pedidos reais nem alteraram dados no Firebase. Integração com operação física/PDV instalado e entrega real ainda requer homologação. Nenhuma ativação V2, implantação de Functions/regras/índices ou mudança de credenciais. Publicação pelo push da main inclui a API Vercel existente.
